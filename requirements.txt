# ============================================================
# AcciEye — Unified Requirements
# All services: Detection, Classification, Agent, UI, Logger
# ============================================================

# ── Computer Vision & Object Detection ──────────────────────
ultralytics>=8.0.0          # YOLOv8 / YOLO26 training & inference
opencv-python-headless>=4.8.0  # Headless OpenCV (no GUI window needed in Docker)
Pillow>=10.0.0              # Image handling

# ── Data & Numeric ──────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0

# ── LangChain & LLM Integration ─────────────────────────────
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langchain-huggingface>=0.0.3
langchain-core>=0.2.0

# ── Vector Database & Embeddings ────────────────────────────
faiss-cpu>=1.7.4            # CPU FAISS (use faiss-gpu if you have NVIDIA GPU)
sentence-transformers>=2.7.0
huggingface-hub>=0.23.0
transformers>=4.40.0

# ── Knowledge Graph ─────────────────────────────────────────
networkx>=3.3               # Deterministic right-of-way graph (TrafficLogicGraph)

# ── PDF Parsing (Legal KB ingestion) ────────────────────────
pdfplumber>=0.11.0
pypdf>=4.0.0
tiktoken>=0.7.0             # Token counting for LLM calls

# ── Gradio UI ───────────────────────────────────────────────
gradio>=4.31.0

# ── HTTP / Webhooks / Automation ────────────────────────────
requests>=2.31.0            # Webhook calls (n8n, Telegram)
python-dotenv>=1.0.0        # .env file loading

# ── PyTorch (CPU) ───────────────────────────────────────────
# For GPU support replace with:
#   torch>=2.1.0+cu121 --extra-index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# ── Utilities ───────────────────────────────────────────────
tqdm>=4.66.0
pyyaml>=6.0.1
albumentations>=1.3.0       # Data augmentation (preprocessing pipeline)
